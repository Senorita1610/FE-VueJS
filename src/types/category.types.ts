export interface CategoryBody {
    name: string,
    description: string,
    slug: string,
}

export interface Category {
    categoryId: number,
    name: string,
    description: string,
    slug: string,
}